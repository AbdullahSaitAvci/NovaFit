@{
    ViewData["Title"] = "Ana Sayfa";
}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;700&display=swap" rel="stylesheet">

<style>
    /* --- 1. GENEL AYARLAR --- */
    :root {
        --nova-yellow: #FFD200; /* MacFit Sarısı */
        --nova-bg: #1a1a1a; /* Mat Antrasit Arka Plan */
        --nova-card: #242424; /* Kart Rengi */
        --nova-text-main: #ffffff; /* Beyaz Yazı */
        --nova-text-sub: #cccccc; /* Açık Gri Yazı */
        --nova-font: 'Oswald', sans-serif;
    }

    body {
        background-color: var(--nova-bg) !important;
        color: var(--nova-text-main) !important;
        font-family: 'Segoe UI', sans-serif;
    }

    /* Font Zorlamaları */
    h1, h2, h3, h4, .btn-nova, .btn-nova-dark {
        font-family: var(--nova-font) !important;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: #ffffff !important; /* Başlıklar kesinlikle beyaz */
    }

    /* --- 2. HERO (ÜST) ALAN --- */
    .hero-wrapper {
        position: relative;
        background: url('https://images.unsplash.com/photo-1534438327276-14e5300c3a48?q=80&w=1470&auto=format&fit=crop');
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        /* Yüksekliği ve dolguyu ayarladık */
        padding: 180px 0 180px 0;
        border-radius: 0 0 50px 50px;
        /* Alttan boşluk bırakmıyoruz, container margin ile halledeceğiz */
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0,0,0,0.5);
    }

    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        /* Yazıların okunması için güçlü bir karartma */
        background: rgba(0, 0, 0, 0.65);
        z-index: 1;
    }

    .hero-content {
        position: relative;
        z-index: 2;
    }

    .hero-title {
        font-size: 5rem; /* Büyük ve Heybetli */
        font-weight: 700;
        text-shadow: 2px 2px 10px rgba(0,0,0,0.9); /* Siyah gölge */
        line-height: 1.1;
    }

    /* --- 3. KARTLAR --- */
    /* Kutular arası boşluk burada ayarlanıyor: margin-top: 50px */
    .features-container {
        margin-top: 60px; /* Üst kısımla aradaki BOŞLUK */
        margin-bottom: 60px;
    }

    .nova-card {
        background-color: var(--nova-card) !important;
        border: 1px solid #333;
        border-radius: 15px;
        padding: 40px 30px;
        transition: transform 0.3s ease;
        height: 100%;
        text-align: center;
        box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    }

        .nova-card:hover {
            transform: translateY(-10px);
            border-color: var(--nova-yellow);
            box-shadow: 0 10px 40px rgba(255, 210, 0, 0.15);
        }

    .icon-box {
        font-size: 3.5rem;
        color: var(--nova-yellow);
        margin-bottom: 25px;
    }

    /* --- 4. BUTONLAR --- */
    .btn-nova {
        background-color: var(--nova-yellow) !important;
        color: #000 !important;
        font-weight: 700;
        padding: 15px 50px;
        border-radius: 5px;
        border: none;
        font-size: 1.2rem;
        transition: 0.3s;
        text-decoration: none;
        display: inline-block;
    }

        .btn-nova:hover {
            background-color: #fff !important;
            transform: scale(1.05);
            box-shadow: 0 0 25px rgba(255, 210, 0, 0.6);
        }

    .btn-nova-dark {
        background-color: transparent !important;
        color: #fff !important;
        border: 2px solid #fff;
        font-weight: 700;
        padding: 13px 40px;
        border-radius: 5px;
        font-size: 1.2rem;
        transition: 0.3s;
    }

        .btn-nova-dark:hover {
            background-color: #fff !important;
            color: #000 !important;
        }

    .text-yellow {
        color: var(--nova-yellow) !important;
        text-shadow: none;
    }

    /* Paragrafların rengini sabitliyoruz */
    p {
        color: var(--nova-text-sub) !important;
        font-size: 1.1rem;
        line-height: 1.6;
    }

    /* Alt Promosyon Kartları */
    .promo-card {
        background-color: var(--nova-card) !important;
        padding: 50px;
        border-radius: 20px;
        border: 1px solid #333;
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }
</style>

<div class="hero-wrapper">
    <div class="hero-overlay"></div>
    <div class="container text-center hero-content">
        <h1 class="hero-title mb-3">
            HAYALİNDEKİ <span class="text-yellow">VÜCUT</span>
        </h1>
        <p class="col-md-8 mx-auto mb-5" style="font-size: 1.3rem;">
            NovaFit ile sınırlarını zorla. Profesyonel yapay zeka desteği ve uzman antrenörlerle değişimi bugün başlat.
        </p>

        @if (!User.Identity.IsAuthenticated)
        {
            <div class="d-flex justify-content-center gap-3">
                <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-nova">
                    HEMEN BAŞLA
                </a>
                <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-nova-dark">
                    GİRİŞ YAP
                </a>
            </div>
        }
        else
        {
            <a asp-controller="FitnessServices" asp-action="Index" class="btn btn-nova">
                <i class="bi bi-calendar-check-fill me-2"></i> RANDEVU AL
            </a>
        }
    </div>
</div>

<div class="container features-container">
    <div class="row g-4">
        <div class="col-md-4">
            <div class="nova-card">
                <div class="icon-box"><i class="bi bi-lightning-charge-fill"></i></div>
                <h3>UZMAN KADRO</h3>
                <p>Sertifikalı ve tecrübeli hocalarımızla güvenle çalış, sakatlanmadan profesyonelce geliş.</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="nova-card">
                <div class="icon-box"><i class="bi bi-graph-up-arrow"></i></div>
                <h3>KİŞİSEL PLAN</h3>
                <p>Senin metabolizmana, yaşam tarzına ve hedeflerine %100 uyumlu, nokta atışı programlar.</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="nova-card">
                <div class="icon-box"><i class="bi bi-cpu-fill"></i></div>
                <h3>YAPAY ZEKA</h3>
                <p>Gelişimini 7/24 takip eden, anlık beslenme ve antrenman revizesi yapan AI asistan.</p>
            </div>
        </div>
    </div>
</div>

<div class="container pb-5">
    <div class="row g-4">
        <div class="col-md-6">
            <div class="promo-card">
                <div class="d-flex align-items-center mb-4">
                    <i class="bi bi-people-fill display-4 me-3 text-yellow"></i>
                    <h2 class="m-0">ANTRENÖRLER</h2>
                </div>
                <p class="mb-4">Seni motive edecek, sınırlarını zorlayacak profesyonel ekibimizle tanışmaya hazır mısın?</p>
                <a asp-controller="Trainers" asp-action="Index" class="btn btn-nova w-100">
                    EKİBİ İNCELE <i class="bi bi-arrow-right ms-2"></i>
                </a>
            </div>
        </div>
        <div class="col-md-6">
            <div class="promo-card">
                <div class="d-flex align-items-center mb-4">
                    <i class="bi bi-tags-fill display-4 me-3 text-yellow"></i>
                    <h2 class="m-0">ÜYELİKLER</h2>
                </div>
                <p class="mb-4">Öğrenci dostu fiyatlar, sınırsız erişim ve özel ders paketleri. Sana en uygun olanı seç.</p>
                <a asp-controller="FitnessServices" asp-action="Index" class="btn btn-nova-dark w-100">
                    FİYATLARI GÖR <i class="bi bi-check-circle-fill ms-2"></i>
                </a>
            </div>
        </div>
    </div>
</div>